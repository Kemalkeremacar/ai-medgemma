# Qdrant Vector Database
# Değiştirdikten sonra: ./svc restart qdrant

QDRANT__SERVICE__HTTP_PORT=6333
QDRANT__SERVICE__GRPC_PORT=6334
RUN_MODE=production
TZ=Etc/UTC

# Veri dizini (container volume olarak bağlanır — docker-compose.yml'den değiştirin)
# Host path: /raid/monassist1/qdrant_storage
